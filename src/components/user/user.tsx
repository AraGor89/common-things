import { Typography } from "@mui/material";

const User = () => {
  return (
    <Typography component="div" variant="h4">
      Here will be the user Page
    </Typography>
  );
};

export default User;
