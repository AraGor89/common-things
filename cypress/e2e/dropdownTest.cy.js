/// <reference types="cypress" />

describe("Dropdown", () => {
  it("dropdown test", () => {
    cy.visit("");

    // cy.get(".gLFyf").type("cypress").invoke('attr')

    // cy.get(".OBMEnb ul li span").contains("cypress typescript");
    //   .click({ force: true });
  });
});
